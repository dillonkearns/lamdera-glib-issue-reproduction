[build]
  publish = "dist/"
  command = "apt-get install -y libtinfo5 && mkdir bin && curl https://static.lamdera.com/bin/linux/lamdera -o ./bin/lamdera && chmod a+x ./bin/lamdera && ./bin/lamdera make src/Main.elm --output dist/index.html"
